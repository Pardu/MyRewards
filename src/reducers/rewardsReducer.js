export const initialState = {
  rewards: [],
  loading: false,
  hasErrors: false,
}

export default function rewardsReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}